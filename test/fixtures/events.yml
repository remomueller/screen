# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/Fixtures.html

one:
  patient: one
  class_name: Prescreen
  class_id: <%= ActiveRecord::Fixtures.identify(:one) %>

two:
  patient: one
  class_name: Mailing
  class_id: <%= ActiveRecord::Fixtures.identify(:one) %>

three:
  patient: one
  class_name: Call
  class_id: <%= ActiveRecord::Fixtures.identify(:one) %>

four:
  patient: one
  class_name: Evaluation
  class_id: <%= ActiveRecord::Fixtures.identify(:one) %>

five:
  patient: one
  class_name: Visit
  class_id: <%= ActiveRecord::Fixtures.identify(:one) %>
