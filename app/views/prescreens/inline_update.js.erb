<% Prescreen::EDITABLES.each do |item| %>
  <% value = (item == 'exclusion') ? @prescreen.send(item+'_name') : @prescreen.send(item) %>
  $('#prescreen_<%= @prescreen.id %>_<%= item %>_inline_edit').html("<%= escape_javascript(value) %>");
<% end %>
