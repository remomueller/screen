<% @title = "Choice: #{@choice.name}" %>
<div class="page-header">
  <h1>
    <%= @title %>
    <% if current_user.screener? %>
      <%= link_to "Edit Choice", edit_choice_path(@choice), class: 'btn btn-mini' %>
      <%= link_to "Delete Choice", @choice, confirm: "Are you sure you want to delete Choice #{@choice.name}?", method: :delete, class: 'btn btn-mini btn-danger' %>
    <% end %>
    <%= link_to "View Choices", choices_path, class: 'btn btn-mini' %>
  </h1>
</div>

<dl class="dl-horizontal">
  <dt>Category</dt>
  <dd><%= @choice.category %></dd>

  <dt>Name</dt>
  <dd><%= @choice.name %></dd>

  <dt>Description</dt>
  <dd><%= @choice.description %></dd>

  <dt>Included Fields</dt>
  <dd>
    <ul>
      <% @choice.fields.each do |field| %>
        <li><%= field %></li>
      <% end %>
    </ul>
  </dd>


  <dt>Color</dt>
  <dd><span class="label" style="background-color: <%= @choice.color %>"><%= @choice.color %></span></dd>
</dl>




