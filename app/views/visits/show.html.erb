<% @title = "Visit #{@visit.name}" %>
<div class="page-header">
  <h1>
    <%= link_to @visit.patient.phi_code(current_user), @visit.patient %> &middot; <%= @title %>
    <% if current_user.screener? or current_user.subject_handler? %>
      <%= link_to "Edit Visit", edit_visit_path(@visit), class: 'btn btn-xs btn-default' %>
      <%= link_to "Delete Visit", @visit, method: :delete, class: 'btn btn-xs btn-danger-inverse', data: { confirm: "Are you sure you want to delete Visit #{@visit.name}?" } %>
    <% end %>
    <%= link_to "View Visits", visits_path, class: 'btn btn-xs btn-default' %>
  </h1>
</div>

<dl class="dl-horizontal">
  <dt>Visit type</dt>
  <dd><%= @visit.visit_type_name %></dd>

  <dt>Visit date</dt>
  <dd><%= simple_date @visit.visit_date %></dd>

  <dt>Outcome</dt>
  <dd><%= @visit.outcome_name %></dd>

  <dt>Comments</dt>
  <dd><%= @visit.comments %></dd>
</dl>
